server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: HAIRFLOW-CONFIG-SERVER
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations