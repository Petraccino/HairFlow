server:
   port: 8771
spring:
  application:
     name: HairFlow-gateway
  cloud:
    gateway:
      routes:
        - id: HairFlow-client
          uri: ${CLIENT_ROUTE_URI:http://localhost:8201}
          predicates:
            - Path=/client**