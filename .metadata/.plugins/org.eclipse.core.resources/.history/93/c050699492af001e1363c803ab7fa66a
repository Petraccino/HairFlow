spring:
 cloud:
   gateway:
     routes:
       - id: HairFlow-client
         uri: lb://HAIRFLOW-CLIENT
         predicates:
           - Path=/client/**

 application:
   name: HAIRFLOW-GATAWAY

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/