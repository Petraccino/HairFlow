spring:
 cloud:
   gateway:
     routes:
       - id: HairFlow-client
         uri: lb://HAIRFLOW-CLIENT
         predicates:
           - Path=/client/**
server:
  port: 8771


  application:
    name: HAIRFLOW-GATAWAY

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/