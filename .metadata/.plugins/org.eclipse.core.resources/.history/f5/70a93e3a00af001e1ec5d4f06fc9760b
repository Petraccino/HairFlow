server:
  port: 8771

spring:
  application:
    name: HairFlow-gateway
  cloud:
    gateway:
      routes:
        - id: HairFlow-client
          uri: 'http://localhost:8201'RESTAURANT-SERVICE
          predicates:
            - Path=/client/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka